# 依赖与构建产物
node_modules/
dist/

# 系统与编辑器
.DS_Store
Thumbs.db
*.log

# Python 缓存（convert-chiuns.py 等脚本运行产生）
__pycache__/
*.pyc

# 注意：素材库（bible_databases 等）位于 site 目录之外，git 不会跟踪，
# 与网站隔离约定一致；public/data 与 data-src 必须提交（CI 依赖，见 docs/DEPLOY.md）
# 测试截图产物（gui 验证用，不入库）
gui-test-screenshots/

# 超大素材（>100MB，GitHub 单文件限制；仅本地数据流水线使用，CI 不依赖）
data-src/geography/normalized/polities.json
